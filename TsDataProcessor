def TsDataProcessor(frame1 = 0, frame2 = 0, frame3 = 0, frame4 = 0, frame5 = 0, frame6 = 0, t = 10):
  
  frames = (frame1, frame2, frame3, frame4, frame5, frame6)
  
  specs = pd.DataFrame([])
  
  for frame in frames:
    if frame !=0:
      specs.append(pd.DataFrame([[frame.index[0], frame.index[-1]]])
  
  start = specs[0].max()
  end = specs[1].min()
  
  
  
