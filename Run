import numpy as np
import pandas as pd
import os
import importlib


os.chdir("/Users/justinmulli/PycharmProjects/Stocks")
import DataProcessor
importlib.reload(DataProcessor)

stocks = pd.read_csv("all_stocks_5yr.csv", index_col = "date")
apple = stocks[stocks["Name"] == "AAPL"]
dex = pd.read_csv("sentdex.csv", index_col = "date")
print(dex.columns.values)
appledex = dex[dex["symbol"] == "AAPL"]
appledex = appledex["sentiment_signal"].to_frame()

combined, test = DataProcessor.TsDataProcessor(apple, appledex)

#combined = combined[combined["sentiment_signal"]!=0]
